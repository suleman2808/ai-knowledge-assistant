# --- Core orchestration -------------------------------------------------
langgraph>=0.2.60
langchain-core>=0.3.25

# --- LLM provider -------------------------------------------------------
# Only the Groq SDK is imported, and only inside app/llm.py.
# Swapping providers means changing that one module, not the whole app.
groq>=0.13.0

# --- Retrieval ----------------------------------------------------------
# Chroma also provides the default embedding runtime: all-MiniLM-L6-v2 as
# an ONNX export on onnxruntime, with no PyTorch dependency.
chromadb>=0.5.20
numpy>=1.26
# The optional PyTorch runtime lives in requirements-torch.txt.

# --- Streamlit front-end ------------------------------------------------
# Only used by streamlit_app.py, which exists so the project can be hosted
# free on Streamlit Community Cloud. That platform installs from this file
# and this file alone, so the dependency lives here rather than in a
# separate one. The FastAPI app does not import it.
streamlit>=1.40.0

# --- HTTP layer ---------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Google Calendar (Booking Agent) ------------------------------------
google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.1
# Windows ships no IANA time zone database, so zoneinfo cannot resolve
# "America/Los_Angeles" without this. Harmless on Linux and macOS.
tzdata>=2024.2

# --- Dev / test ---------------------------------------------------------
pytest>=8.3.0
httpx>=0.27.0
python-dateutil>=2.9.0

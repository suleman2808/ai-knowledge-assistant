# Streamlit Community Cloud: paste this under Settings -> Secrets.
# Locally, .env is used instead and this file is unnecessary.
#
# Never commit a real secrets.toml — it is gitignored.

GROQ_API_KEY = "gsk_replace_me"
